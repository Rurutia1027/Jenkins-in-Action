pipeline {
    agent any

    tools {
        go 'go-1.21'
    }

    stages {
        stage('Unit Tests -- Backend') {
            steps {
                dir('bugtracker-backend') {
                    sh 'go test -v ./...'
                }
            }
        }
    }
}
